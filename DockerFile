FROM python:3.10-slim

ENV PORT 7860
EXPOSE 7860

# Set working directory
WORKDIR /app

# Copy all files into /app
COPY . .

# Make a .streamlit folder inside /app with correct permissions
RUN mkdir -p /app/.streamlit

# Set environment variable to force Streamlit to use the app folder
ENV STREAMLIT_CONFIG_DIR=/app/.streamlitFROM python:3.10-slim

# Environment config
ENV PORT 7860
EXPOSE 7860

# Set working directory
WORKDIR /app

# Copy all files
COPY . .

# Create writable .streamlit directory in /app
RUN mkdir -p /app/.streamlit

# Tell Streamlit to use that directory
ENV STREAMLIT_CONFIG_DIR=/app/.streamlit

# Install dependencies
RUN pip install --upgrade pip
RUN pip install -r requirements.txt

# Launch the app
CMD ["streamlit", "run", "app.py", "--server.port=7860", "--server.address=0.0.0.0"]

# Install requirements
RUN pip install --upgrade pip
RUN pip install -r requirements.txt

# Run Streamlit app
CMD ["streamlit", "run", "app.py", "--server.port=7860", "--server.address=0.0.0.0"]
